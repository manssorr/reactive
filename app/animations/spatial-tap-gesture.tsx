import { StyleSheet, Text, View } from 'react-native';

interface IProps {}

const SpatialTapGesture = (props: IProps): React.ReactElement<IProps> => {
  return (
    <View>
      <Text>SpatialTapGesture</Text>
    </View>
  );
};

export default SpatialTapGesture;

const styles = StyleSheet.create({});
